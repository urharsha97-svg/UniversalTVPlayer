name: Build UniversalTVPlayer V3

on:
  workflow_dispatch:
  push:
    paths:
      - "UniversalTVPlayer-V3.zip"
      - ".github/workflows/build.yml"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10.2"

      - name: Extract V3
        run: |
          rm -rf player
          mkdir player
          unzip -q UniversalTVPlayer-V3.zip -d player
          echo "V3 extracted:"
          find player -maxdepth 4 -type f | head -150

      - name: Build APK
        run: |
          gradle -p player :app:assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: UniversalTVPlayer-V3-APK
          path: player/app/build/outputs/apk/debug/app-debug.apk
